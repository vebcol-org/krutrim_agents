"""Document parsing for RAG ingestion — plain text/markdown plus PDF/DOCX
via docling. See `registry.default_registry()` for the entry point
`krutrim_agent_celery`'s `process_rag_document` task calls."""
