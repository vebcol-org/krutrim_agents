# agent-renderers

This library was generated with [Nx](https://nx.dev).

Published as `@krutrim_agent/agent-renderers` — per-agent-type canvas renderers + the `getAgentRenderer(agentKey)` registry. See the root [`README.md`](../../README.md#reusable-packages--consuming-this-repo-from-another-codebase) for how an external repo consumes this package.

## Running unit tests

Run `nx test agent-renderers` to execute the unit tests via [Jest](https://jestjs.io).
