# agent-ui

This library was generated with [Nx](https://nx.dev).

Published as `@krutrim_agent/agent-ui` — the top-level `<Agent>` product shell. See the root [`README.md`](../../README.md#reusable-packages--consuming-this-repo-from-another-codebase) for how an external repo consumes this package, and its `extensions` prop for wiring in real auth/visibility hooks.

## Running unit tests

Run `nx test agent-ui` to execute the unit tests via [Jest](https://jestjs.io).
