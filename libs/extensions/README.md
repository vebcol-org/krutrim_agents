# extensions

This library was generated with [Nx](https://nx.dev).

Frontend mirror of the backend's `krutrim_agent_extensions` package — security-hook contracts, an `ExtensionProvider` React context, and a runtime drift-check banner (`ExtensionSelfCheck`). See the root [`README.md`](../../README.md) for how a consuming app wires this in via `<Agent extensions={...}>`.
