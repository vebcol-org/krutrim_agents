# ui

This library was generated with [Nx](https://nx.dev).

Published as `@krutrim_agent/ui` — shared React primitives + the Tailwind v4 theme (`theme.css`, exported unprocessed as `@krutrim_agent/ui/theme.css`). See the root [`README.md`](../../README.md#reusable-packages--consuming-this-repo-from-another-codebase) for external consumption + the required `@source` setup.

## Running unit tests

Run `nx test ui` to execute the unit tests via [Jest](https://jestjs.io).
